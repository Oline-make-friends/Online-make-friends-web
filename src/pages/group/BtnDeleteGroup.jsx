import React from "react";
import axios from "axios";

const GroupDetail = () => {
  return (
    <button
      onClick={() => {
        console.log("delete");
      }}
    >
      BtnDeleteGroup
    </button>
  );
};

export default GroupDetail;
